#include <iostream>

int main() {
  std::string d;
  std::cout << "Enter a string: ";
  std::cin >> d;
  int l = 0;
  while (d[l] != '\0') {
      l++;
  }
  for(int i = l - 1; i >= 0; i--){
    std::cout << d[i];
  }
}
